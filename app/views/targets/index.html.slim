h1.l-dashboard__heading 目標管理

.u-mt-15 = link_to '目標を作成する', new_target_path, class: 'c-btnMain-standard c-btn-blue'

.c-table.u-mt-30
  table
    thead
      tr
        th ID
        th 部署
        th 年
        th 月
        th 売上
        th 利益
        th コスト
        th colspan=3
    tbody
      - targets.each do |r|
        tr
          td = r.id
          td = r.division.name
          td = r.target_year
          td = r.target_month
          td.u-ta-right = "¥ #{r.sales.to_s(:delimited)}"
          td.u-ta-right = "¥ #{r.profit.to_s(:delimited)}"
          td.u-ta-right = "¥ #{r.cost.to_s(:delimited)}"
          td = link_to '編集', edit_target_path(r), class: 'c-btnMain-standard'
          td = link_to '削除', target_path(r), class: 'c-btnMain-negative', method: :delete, data: { confirm: '本当に削除しますか?' }

= render 'layouts/_partials/pagination'
