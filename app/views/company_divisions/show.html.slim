= react_component :ShowCompanyDivision, { company_name: division.company.name, division: division, csrf_params: request_forgery_protection_token, csrf_token: form_authenticity_token }

= react_component :CompanyDivisionClientList, { company_name: division.company.name, division_name: division.name, division_id: division.id, clients: clients, params_name: params[:name] }

javascript:
  
  document.getElementById('js-drawerOpen').click()