h1.l-dashboard__heading 製造経費情報一覧

= link_to '労務費を入力する', new_labor_path, class: 'c-btnMain-standard c-btn-blue u-mt-15'

.c-table.u-mt-30
  table
    thead
      tr
        th 申請日
        th 部署
        th 金額(税抜き)
        th colspan= 2
    tbody
      - labors.each do |r|
        tr
          td = r.date
          td = r.division.name
          td = r.price.to_s(:delimited)
          td = link_to '編集', edit_labor_path(r), class: 'c-btnMain-standard'
          td = link_to '削除', labor_path(r), class: 'c-btnMain-negative', method: :delete, data: { confirm: '本当に削除しますか?' }


= render 'layouts/_partials/pagination'
