json = { status: :success }

json[:flags] = ContentFlag.all.search(params[:name]).where(content_type: params[:content_type]).map do |r|
  {
    id: r.id,
    name: r.name,
    content_type: r.content_type
  }
end

json