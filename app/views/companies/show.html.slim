= react_component :ShowCompany, { company: company, csrf_params: request_forgery_protection_token, csrf_token: form_authenticity_token }

= react_component :CompanyDivisionList, { company_id: company.id, divisions: divisions, params_name: params[:name] }

javascript:

  document.getElementById('js-drawerOpen').click()