json = { status: :success }

json[:divisions] = divisions.where("(id = ?) OR (id = ?) OR (id = ?) OR (id = ?) OR (id = ?) OR (id = ?)", 1, 3, 5, 6, 7, 13).map do |r|

  {
    id: r.id,
    name: r.name,
  }
end

json
