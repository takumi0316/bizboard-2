= react_component :ShowCompanyDivisionClient, { company_name: client.company_division.company.name, company_id: client.company_division.company.id, divisions: client.company_division.company.divisions, division: client.company_division, users: User.where(status: :active), client: client, csrf_params: request_forgery_protection_token, csrf_token: form_authenticity_token }

javascript:
  
  document.getElementById('js-drawerOpen').click()